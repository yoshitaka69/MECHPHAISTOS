import {
  staticRegister
} from "./chunk-DSUKSR6K.js";

// node_modules/handsontable/renderers/registry.mjs
var {
  register,
  getItem,
  hasItem,
  getNames,
  getValues
} = staticRegister("renderers");
function _getItem(name) {
  if (typeof name === "function") {
    return name;
  }
  if (!hasItem(name)) {
    throw Error(`No registered renderer found under "${name}" name`);
  }
  return getItem(name);
}
function _register(name, renderer) {
  if (typeof name !== "string") {
    renderer = name;
    name = renderer.RENDERER_TYPE;
  }
  register(name, renderer);
}

export {
  hasItem,
  getNames,
  getValues,
  _getItem,
  _register
};
//# sourceMappingURL=chunk-FW2LINYA.js.map
