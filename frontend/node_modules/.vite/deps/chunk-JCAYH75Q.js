import {
  staticRegister
} from "./chunk-ST45JAFY.js";

// node_modules/handsontable/validators/registry.mjs
var {
  register,
  getItem,
  hasItem,
  getNames,
  getValues
} = staticRegister("validators");
function _getItem(name) {
  if (typeof name === "function") {
    return name;
  }
  if (!hasItem(name)) {
    throw Error(`No registered validator found under "${name}" name`);
  }
  return getItem(name);
}
function _register(name, validator) {
  if (typeof name !== "string") {
    validator = name;
    name = validator.VALIDATOR_TYPE;
  }
  register(name, validator);
}

export {
  hasItem,
  getNames,
  getValues,
  _getItem,
  _register
};
//# sourceMappingURL=chunk-JCAYH75Q.js.map
